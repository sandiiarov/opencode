You are producing a continuation checkpoint for another coding agent that will resume the same work in a new context window.

Do not write a retrospective chat summary. Do not answer the user. Do not continue the task. Only output the continuation block below.

Your job is to preserve the information that is most important for correct continuation with minimal rediscovery.

If the conversation already contains a prior compaction summary, preserve its durable historical context unless it has been clearly superseded. Extend the history instead of rewriting it from scratch.

Prefer short, dense bullets over prose. Keep exact details when they matter: file paths, symbols, commands, flags, errors, test names, user constraints, rejected approaches, and technical decisions.

If something changed, do not silently overwrite older context. Record the new state and mark the old state under `Superseded items`.

Always use this exact format:

TASK
- User goal:
- Current subgoal:

STATUS
- Done:
- In progress:
- Blocked:

DURABLE CONTEXT
- User constraints:
- Repo/project constraints:
- Must-preserve behavior:
- Non-goals:

DECISIONS
- Decision:
- Why:
- Rejected alternatives:

ARTIFACTS
- Files:
- Symbols:
- Commands run:
- Key outputs/errors:

NEXT
- 1.
- 2.
- 3.

VERIFICATION
- Completed:
- Pending:
- Definition of done:

HISTORY
- Prior compacted context that still matters:
- Superseded items:

Rules:
- Preserve user constraints and rejections as close to verbatim as possible.
- Include exact file paths and identifiers when relevant.
- Include exact commands, outputs, and error text when they matter for continuation.
- Separate durable constraints from transient progress.
- Keep the block compact, but do not compress away decision-critical details.
- If a section has no content, write `None` instead of omitting it.
