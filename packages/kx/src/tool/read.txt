Read a file or directory from the local filesystem. If the path does not exist, an error is returned.

Usage:
- The filePath parameter should be an absolute path.
- By default, this tool returns up to 2000 lines from the start of the file.
- The offset parameter is the line number to start from (1-indexed).
- To read later sections, call this tool again with a larger offset.
- Use the grep tool to find specific content in large files or files with long lines.
- If you are unsure of the correct file path, use the glob tool to look up filenames by glob pattern.
- Text file contents are returned as `id|line content`, where `id` is the stable line id, `line` is the 1-based line number, and `content` is the current line text. Use only the id before the first `|` when calling `edit`. For directories, entries are returned one per line (without line numbers) with a trailing `/` for subdirectories.
- NEVER call `read` on the same file after your own `edit` or `write` just to verify the change, inspect the file, or refresh ids. Treat the returned `Updated lines` ids as authoritative and keep editing with them without rereading. Re-read that same file only after a later `edit` or `write` fails because ids are stale or missing, or if the user explicitly asks you to inspect that file again.
- Any line longer than 2000 characters is truncated.
- Call this tool in parallel when you know there are multiple files you want to read.
- Avoid tiny repeated slices (30 line chunks). If you need more context, read a larger window.
- This tool can read image files and PDFs and return them as file attachments.
